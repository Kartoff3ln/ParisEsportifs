{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <!-- Redirection vers la liste des messages pour les Admins -->
    {% if is_granted("ROLE_ADMIN") %}
        <script>
                window.location.href = '/message';
        </script>
    {% endif %}

    {{ include('message/_form.html.twig') }}
{% endblock %}
